import styled from 'styled-components';

export const LogoContainer = styled.div`
    display: flex;
    flex-direction: row;
    gap: 8px;
    align-items: center;

    height: 30px;

    h3 {
        color: var(--color-secondary);
        align-self: flex-end;
    }
`;
